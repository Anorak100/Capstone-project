# Fhast Pay Backend

This directory contains the API for the Fhast Pay capstone project.

## Local setup

1. Install dependencies with `npm install`.
2. Copy `.env.example` to `.env` and set local environment values.
3. Add the application start script when the backend implementation is ready.

Environment files containing real credentials are ignored by Git. Use `.env.example` to document required variables safely.
